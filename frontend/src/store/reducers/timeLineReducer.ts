// import { TimeLineActionTypes, TimeLineState, ChangeAction } from '../../types/timeline';

// const initialState: TimeLineState = {
//   time: null,
//   instrument: null,
// };

// export const timeLineReducer = (state = initialState, action: ChangeAction): TimeLineState => {
//   switch (action.type) {
//     case TimeLineActionTypes.CHANGE_TIME:
//       return { time: action.payload, instrument: null };
//     case TimeLineActionTypes.CHANGE_INSTRUMENT:
//       return { time: null, instrument: action.payload };
//     default:
//       return state;
//   }
// };
